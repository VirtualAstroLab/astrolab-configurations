server:
        port: 9091

eureka:
        client:
                registerWithEureka: true
                fetchRegistry: false
                serviceUrl:
                        defaultZone: http://astrolab-eureka-server:9091/eureka/
      
        server:
                wait-time-in-ms-when-sync-empty: 3000
